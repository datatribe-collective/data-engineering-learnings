# -----------------------------------------------------------------------------
# AIRFLOW_UID
#
# Recommended usage:
#    export AIRFLOW_UID=$(id -u)
# Or on Windows PowerShell:
#    setx AIRFLOW_UID $($env:USERNAME)
#
# Leave blank here so it can be overridden per machine
AIRFLOW_UID=

# -----------------------------------------------------------------------------
# Airflow Configuration
# -----------------------------------------------------------------------------

# Executor type (CeleryExecutor recommended for production-like local dev)
AIRFLOW_CORE__EXECUTOR=CeleryExecutor

# Database connection string
AIRFLOW_DATABASE__SQL_ALCHEMY_CONN=postgresql+psycopg2://airflow:airflow@postgres/airflow

# -----------------------------------------------------------------------------
# Google Cloud / BigQuery - REQUIRED
#
# Replace these with your actual GCP project details
# -----------------------------------------------------------------------------

# Google Cloud project ID
BQ_PROJECT_ID=your-gcp-project-id

# BigQuery dataset name
BQ_DATASET=your-bq-dataset

# BigQuery raw table name
BQ_TABLE=your-bq-table

# BigQuery summary table name
BQ_SUMMARY_TABLE=your-bq-summary-table


# Path to your GCP service account JSON key (inside the container)
GOOGLE_APPLICATION_CREDENTIALS_PATH=/opt/airflow/creds/gcp_service_account.json

# -----------------------------------------------------------------------------
# Docker Image Repo Prefix
# -----------------------------------------------------------------------------

# Customize this to your Docker Hub or GCR repo prefix if needed
# If you wanna my prebuilt images from Docker Hub, set this to:
DOCKER_IMAGE_PREFIX=bravo634

# -----------------------------------------------------------------------------
# Airflow Web Portal Credentials
# -----------------------------------------------------------------------------

# Airflow default admin user
_AIRFLOW_WWW_USER_USERNAME=airflow
_AIRFLOW_WWW_USER_PASSWORD=airflow